---
const { header } = Astro.props;
---

<div
  class={`celularPequeno:w-full celularPequeno:flex celularPequeno:flex-col celularPequeno:items-center  max-w-48 m-2 text-left `}
>
  <h3 class="text-xl mb-3">{header}</h3>
  <slot />
</div>
