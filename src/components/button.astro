---
const { href, type, classList, id } = Astro.props;
---

{
  href ? (
    <a
      href={href}
      id={id}
      class={`celular:w-10/12 rounded-md text-center block w-2/5 py-2  bg-anaranjado text-black text-base font-audiowide  border-2 border-black hover:bg-transparent hover:text-white hover:border-blanco ${classList || ""}`}
    >
      <slot />
    </a>
  ) : (
    <button
      type={type}
      id={id}
      class={`celular:w-10/12 rounded-md text-center w-2/5 py-2  bg-negro text-white text-base font-audiowide  font-bold border-2 border-white hover:border-blanco hover:bg-transparent hover:text-negro ${classList || ""}`}
    >
      <slot />
    </button>
  )
}
