---
const { label } = Astro.props;
const path = `/${label.replace(/\s+/g, "")}`;
---

<a
  href={path}
  class="font-semibold px-5 my-2 no-underline text-black block hover:text-red-600"
  >{label}</a
>
