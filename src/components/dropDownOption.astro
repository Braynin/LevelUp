---
const { label } = Astro.props;
const path = `/${label.replace(/\s+/g, "")}`;
---

<a
  href={path}
  class="font-syne px-5 my-2 no-underline text-white block hover:text-anaranjado"
  >{label}</a
>
